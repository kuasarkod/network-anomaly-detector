# Core API settings
APP_NAME=AnomalyDetector
APP_ENV=development
APP_DEBUG=true
API_HOST=0.0.0.0
API_PORT=8080
CORS_ORIGINS=http://localhost:5173

# Database
DB_HOST=postgres
DB_PORT=5432
DB_NAME=anomaly
DB_USER=anomaly
DB_PASSWORD=anomaly

# Redis
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0

# Kafka
KAFKA_BOOTSTRAP_SERVERS=kafka:9092
KAFKA_ALERT_TOPIC=anomaly-alerts
KAFKA_EVENTS_TOPIC=anomaly-events

# Queue backend
QUEUE_BACKEND=redis  # options: redis, kafka, memory
QUEUE_STREAM_NAME=anomaly-events
QUEUE_MAX_RETRIES=3

# Enrichment
GEOIP_DB_PATH=
ASN_DB_PATH=

# Alert channels
SLACK_WEBHOOK_URL=
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
SMTP_HOST=
SMTP_PORT=
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=

# Security
JWT_SECRET=change-me
JWT_EXPIRES_IN=3600
